name: terradem

channels:
        - conda-forge

dependencies:
        - python>=3
        - pip
        - scipy
        - matplotlib
        - tqdm
        - geopandas
        - opencv
        - git
        - pytest
        - pip:
                - pytransform3d
                - git+https://github.com/GlacioHack/xdem.git

